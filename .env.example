# Server (로컬 개발 시 server 폴더에서 npm run dev 할 때)
PORT=3000
NODE_ENV=development

# Database (docker-compose 사용 시에는 docker-compose.yml에 정의)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/psychpaper

# Admin JWT (나중에 추가)
# JWT_SECRET=your-secret-key
